<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>ru.dagxam</groupId>
    <artifactId>ForgeUpgrade</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ForgeUpgrade</name>
    <description>Русский плагин улучшения оружия, брони и инструментов для Bukkit 26.2 и Java 26.</description>

    <properties>
        <!-- Основная версия Java для компиляции. -->
        <java.version>26</java.version>
        <maven.compiler.release>26</maven.compiler.release>

        <!-- Кодировка всех исходников, конфигов и русских сообщений. -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <repositories>
        <!-- Официальный репозиторий Spigot API. -->
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
    </repositories>

    <dependencies>
        <!--
            API предоставляется сервером и не включается внутрь JAR плагина.
            Для версии Minecraft/Bukkit 26.2 используется артефакт 26.2-R0.1-SNAPSHOT.
        -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>26.2-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>ForgeUpgrade</finalName>

        <plugins>
            <!-- Компиляция строго Maven + Java 26. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>26</release>
                    <encoding>UTF-8</encoding>
                    <showWarnings>true</showWarnings>
                    <parameters>true</parameters>
                </configuration>
            </plugin>

            <!-- Создание обычного лёгкого JAR без лишних библиотек. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
